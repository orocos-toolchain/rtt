
IF (ENABLE_SCRIPTING)
  FILE( GLOB CPPS [^.]*.cpp )
  FILE( GLOB HPPS [^.]*.hpp [^.]*.h [^.]*.inl)

  GLOBAL_ADD_INCLUDE( rtt/scripting ${HPPS})
  GLOBAL_ADD_SRC( ${CPPS})
  SET( ENV{GLOBAL_SCRIPTING_SRCS} "${CPPS}" )
  SET_SOURCE_FILES_PROPERTIES( ${SRCS} PROPERTIES COMPILE_FLAGS "-fvisibility=hidden")

ENDIF (ENABLE_SCRIPTING)
