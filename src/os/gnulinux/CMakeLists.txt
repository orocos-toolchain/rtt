IF (OROCOS_TARGET STREQUAL "gnulinux")
  FILE( GLOB CPPS [^.]*.c* )
  FILE( GLOB HPPS [^.]*.h* )

  GLOBAL_ADD_INCLUDE( os/gnulinux ${HPPS})
  GLOBAL_ADD_SRC( ${CPPS})
ENDIF (OROCOS_TARGET STREQUAL "gnulinux")