
1. use locking in cpcore_lvd.c file in read and write to fifos
   (interrupt + spinlock)
2. build system for rtai must compile all into canbus_cpp-rt.o
3. build system for lxrt must compile libcanbus_cpp-rt.a for
   our CANPieController and the C++ stuff and a the CANPieController
   for rtai and CANPie for rtai (?).
4. Configurator manager
5. LXRT call to CpMessageWrite

